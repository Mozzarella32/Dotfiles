title   Arch Linux (fallback initramfs)
linux   /arch/vmlinuz-linux.signed
initrd  /arch/initramfs-linux-fallback.img
options root=UUID=REPLACE_ROOT_PART_REPLACE rw rootflags=subvol=@ nvidia_drm.modeset=1
